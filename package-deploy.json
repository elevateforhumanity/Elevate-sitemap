{
  "name": "elevate-full-stack",
  "version": "1.0.0",
  "description": "Elevate for Humanity - Full Stack Deployment",
  "main": "server-combined.js",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma migrate deploy && cd .. && node server-combined.js",
    "dev": "node server-combined.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": "20.x"
  }
}
